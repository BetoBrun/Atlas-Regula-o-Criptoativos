const I18N = {
  en: {
    eyebrow: 'Global regulatory intelligence',
    title: 'Crypto Regulation Atlas',
    subtitle: 'A comparative observatory of crypto regulation, stablecoins, tokenization, and CBDC policy.',
    searchLabel: 'Search', searchPlaceholder: 'Search a country', region: 'Region', allRegions: 'All regions',
    trend: 'Trend', allTrends: 'All trends', improving: 'Improving', stable: 'Stable', worsening: 'Worsening', mixedTrend: 'Mixed',
    scoreMin: 'Minimum score', topic: 'Topic', allTopics: 'All topics', exchanges: 'Exchanges', stablecoins: 'Stablecoins', tokenization: 'Tokenization', cbdc: 'CBDC',
    resetFilters: 'Reset filters', mapView: 'Map view', tableView: 'Table view', mapTitle: 'Global map',
    legendVeryFavorable: '80–100 Very favorable', legendFavorable: '65–79 Favorable', legendMixed: '50–64 Mixed', legendRestrictive: '35–49 Restrictive', legendVeryRestrictive: '0–34 Very restrictive',
    tableTitle: 'Countries table', country: 'Country', score: 'Score', classification: 'Classification', topFavorable: 'Top 10 favorable', topRestrictive: 'Restrictive & mixed',
    methodologyTitle: 'Methodology', methodologyP1: 'The Atlas uses a weighted additive multicriteria score. Eight legal-institutional dimensions are coded at the jurisdiction level with fixed weights summing to 100 points: regulatory clarity, private innovation openness, investor/user protection, institutional coherence, stablecoin regime, tokenization legal security, supervisory capacity and state-centralization risk.', methodologyP2: 'Scores are anchored to explicit levels ranging from absence/prohibition to operative and relatively comprehensive regulation. Official in-force sources prevail over speeches or consultations. Taxation and mining/hardware are tracked outside the central score, which must be read together with trend, summary and FSB implementation stage.',
    selectCountry: 'Select a country', selectCountryHelp: 'Click a country on the map or choose one in the table to inspect its regulatory profile.', latestUpdates: 'Latest updates',
    lastUpdate: 'Last update', statusOverview: 'Status overview', principalLaws: 'Principal laws and frameworks', opportunities: 'Opportunities', risks: 'Risks', sources: 'Sources', criteriaBreakdown: 'Criteria breakdown',
    noData: 'No data available', generatedAt: 'Generated at', countriesCovered: 'Countries covered', mapCoverageNote: 'Filled polygons: {filled} • Marker jurisdictions: {markers}', markerJurisdictions: 'Special markers', markerTooltip: 'Marker jurisdiction',
    jurisdictionsMapped: 'Jurisdictions mapped', normativeActs: 'Normative acts catalogued', themesCompared: 'Themes compared', thesisCore: 'Thesis core jurisdictions',
    themesTitle: 'Thematic pillars', compareTitle: 'Compare jurisdictions', jurisdictionA: 'Jurisdiction A', jurisdictionB: 'Jurisdiction B',
    summary: 'Summary', regulatoryModel: 'Regulatory model', authorities: 'Authorities', stablecoinRegime: 'Stablecoin regime', tokenizationRegime: 'Tokenization regime', cbdcNote: 'CBDC note',
    detailedAnalysis: 'Analytical note', thesisBadge: 'Thesis core',
    class_very_favorable: 'Very favorable', class_favorable: 'Favorable', class_mixed: 'Mixed', class_restrictive: 'Restrictive', class_very_restrictive: 'Very restrictive',
    t_improving: 'Improving', t_stable: 'Stable', t_worsening: 'Worsening', t_mixed: 'Mixed',
    regulated: 'Regulated', 'regulated with litigation risk': 'Regulated with litigation risk', 'developing framework': 'Developing framework', 'open with compliance': 'Open with compliance', 'licensed': 'Licensed', 'licensed framework': 'Licensed framework', 'open with rules': 'Open with rules', 'specialized framework': 'Specialized framework', 'operating': 'Operating', 'uncertain': 'Uncertain', 'banned': 'Banned', 'allowed with friction': 'Allowed with friction', 'partially open': 'Partially open', 'regulated under mica': 'Regulated under MiCA', 'highly favorable': 'Highly favorable', 'very active': 'Very active', 'active': 'Active', 'sandboxing': 'Sandboxing', 'developing': 'Developing', 'open': 'Open', 'incipient': 'Incipient', 'active reform': 'Active reform', 'moderate friction': 'Moderate friction', 'moderate': 'Moderate', 'competitive': 'Competitive', 'favorable': 'Favorable', 'favorable in several states': 'Favorable in several states', 'used informally': 'Used informally', 'heavily': 'Heavy', 'heavy': 'Heavy', 'secondary issue': 'Secondary issue', 'potentially favorable': 'Potentially favorable', 'limited': 'Limited', 'neutral': 'Neutral', 'hostile': 'Hostile', 'pilot under debate': 'Pilot under debate', 'research only': 'Research only', 'euro area exploration': 'Euro area exploration', 'research': 'Research', 'wholesale focus': 'Wholesale focus', 'wholesale and cross-border exploration': 'Wholesale and cross-border exploration', 'not central': 'Not central', 'retail launched with low adoption': 'Retail launched with low adoption', 'advanced state model': 'Advanced state model', 'retail pilot': 'Retail pilot', 'consultation stage': 'Consultation stage',
    'dac8 reporting': 'DAC8 reporting', 'limited relevance': 'Limited relevance', experiencesTitle: 'Regulatory experiences', libraryTitle: 'Research base', timelineTitle: 'Regulatory timeline', overviewNav: 'Overview', experiencesNav: 'Experiences', themesNav: 'Themes', mapNav: 'Map', compareNav: 'Compare', libraryNav: 'Library', timelineNav: 'Timeline', methodologyNav: 'Methodology', openDetails: 'Open details', close: 'Close', filterAll: 'All', filterThesis: 'Thesis core', filterEmerging: 'EMDE focus', filterImplementation: 'Implementation', filterData: 'Data & supervision', byPublisher: 'Publisher', byJurisdiction: 'Jurisdiction', byYear: 'Year', featuredJurisdictions: 'Featured jurisdictions', relatedExamples: 'Examples', themePerspective: 'Why it matters', researchSignals: 'Research signals'
  },
  pt: {
    eyebrow: 'Inteligência regulatória global',
    title: 'Atlas da Regulação de Criptoativos',
    subtitle: 'Um observatório comparado da regulação de criptoativos, stablecoins, tokenização, infraestrutura de mineração e política de CBDC.',
    searchLabel: 'Busca', searchPlaceholder: 'Buscar um país', region: 'Região', allRegions: 'Todas as regiões',
    trend: 'Tendência', allTrends: 'Todas as tendências', improving: 'Em melhora', stable: 'Estável', worsening: 'Em piora', mixedTrend: 'Mista',
    scoreMin: 'Pontuação mínima', topic: 'Tema', allTopics: 'Todos os temas', exchanges: 'Exchanges', stablecoins: 'Stablecoins', tokenization: 'Tokenização', cbdc: 'CBDC',
    resetFilters: 'Limpar filtros', mapView: 'Mapa', tableView: 'Tabela', mapTitle: 'Mapa global',
    legendVeryFavorable: '80–100 Muito favorável', legendFavorable: '65–79 Favorável', legendMixed: '50–64 Misto', legendRestrictive: '35–49 Restritivo', legendVeryRestrictive: '0–34 Muito restritivo',
    tableTitle: 'Tabela de países', country: 'País', score: 'Pontuação', classification: 'Classificação', topFavorable: 'Top 10 favoráveis', topRestrictive: 'Mais mistos/restritivos',
    methodologyTitle: 'Metodologia', methodologyP1: 'O Atlas utiliza score multicritério ponderado. Oito dimensões jurídico-institucionais são codificadas no nível da jurisdição com pesos fixos que somam 100 pontos: clareza regulatória, abertura à inovação privada, proteção do investidor/usuário, coerência institucional, regime de stablecoins, segurança jurídica para tokenização, capacidade supervisória e risco de centralização estatal.', methodologyP2: 'As notas seguem âncoras explícitas que vão da ausência ou proibição até regulação operativa e relativamente abrangente. Fontes oficiais já vigentes prevalecem sobre consultas ou discursos. Tributação e mineração/hardware são acompanhados fora da nota central, que deve ser lida em conjunto com tendência, resumo e estágio de implementação do FSB.',
    selectCountry: 'Selecione um país', selectCountryHelp: 'Clique em um país no mapa ou escolha um na tabela para inspecionar o seu perfil regulatório.', latestUpdates: 'Atualizações recentes',
    lastUpdate: 'Última atualização', statusOverview: 'Visão geral do status', principalLaws: 'Principais leis e marcos', opportunities: 'Oportunidades', risks: 'Riscos', sources: 'Fontes', criteriaBreakdown: 'Detalhamento dos critérios',
    noData: 'Sem dados disponíveis', generatedAt: 'Gerado em', countriesCovered: 'Países cobertos', mapCoverageNote: 'Polígonos preenchidos: {filled} • Jurisdições em marcador: {markers}', markerJurisdictions: 'Marcadores especiais', markerTooltip: 'Jurisdição em marcador',
    jurisdictionsMapped: 'Jurisdições mapeadas', normativeActs: 'Atos normativos catalogados', themesCompared: 'Temas comparados', thesisCore: 'Jurisdições centrais da tese',
    themesTitle: 'Pilares temáticos', compareTitle: 'Comparar jurisdições', jurisdictionA: 'Jurisdição A', jurisdictionB: 'Jurisdição B',
    summary: 'Resumo', regulatoryModel: 'Modelo regulatório', authorities: 'Autoridades', stablecoinRegime: 'Regime de stablecoins', tokenizationRegime: 'Regime de tokenização', cbdcNote: 'Nota sobre CBDC',
    detailedAnalysis: 'Nota analítica', thesisBadge: 'Núcleo da tese',
    class_very_favorable: 'Muito favorável', class_favorable: 'Favorável', class_mixed: 'Misto', class_restrictive: 'Restritivo', class_very_restrictive: 'Muito restritivo',
    t_improving: 'Em melhora', t_stable: 'Estável', t_worsening: 'Em piora', t_mixed: 'Mista',
    regulated: 'Regulado', 'regulated with litigation risk': 'Regulado com risco contencioso', 'developing framework': 'Arcabouço em desenvolvimento', 'open with compliance': 'Aberto com compliance', 'licensed': 'Licenciado', 'licensed framework': 'Regime licenciado', 'open with rules': 'Aberto com regras', 'specialized framework': 'Regime especializado', 'operating': 'Operando', 'uncertain': 'Incerto', 'banned': 'Proibido', 'allowed with friction': 'Permitido com fricções', 'partially open': 'Parcialmente aberto', 'regulated under mica': 'Regulado sob o MiCA', 'highly favorable': 'Muito favorável', 'very active': 'Muito ativo', 'active': 'Ativo', 'sandboxing': 'Em sandbox', 'developing': 'Em desenvolvimento', 'open': 'Aberto', 'incipient': 'Incipiente', 'active reform': 'Reforma em curso', 'moderate friction': 'Atrito moderado', 'moderate': 'Moderado', 'competitive': 'Competitivo', 'favorable': 'Favorável', 'favorable in several states': 'Favorável em vários estados', 'used informally': 'Usado informalmente', 'heavily': 'Pesado', 'heavy': 'Pesado', 'secondary issue': 'Questão secundária', 'potentially favorable': 'Potencialmente favorável', 'limited': 'Limitado', 'neutral': 'Neutro', 'hostile': 'Hostil', 'pilot under debate': 'Piloto em debate', 'research only': 'Apenas pesquisa', 'euro area exploration': 'Exploração na área do euro', 'research': 'Pesquisa', 'wholesale focus': 'Foco em atacado', 'wholesale and cross-border exploration': 'Exploração em atacado e transfronteiriça', 'not central': 'Não central', 'retail launched with low adoption': 'Varejo lançado com baixa adoção', 'advanced state model': 'Modelo estatal avançado', 'retail pilot': 'Piloto de varejo', 'consultation stage': 'Fase de consulta',
    'dac8 reporting': 'Reporte DAC8', 'limited relevance': 'Relevância limitada', experiencesTitle: 'Experiências regulatórias', libraryTitle: 'Base de pesquisa', timelineTitle: 'Linha do tempo regulatória', overviewNav: 'Visão geral', experiencesNav: 'Experiências', themesNav: 'Temas', mapNav: 'Mapa', compareNav: 'Comparar', libraryNav: 'Biblioteca', timelineNav: 'Linha do tempo', methodologyNav: 'Metodologia', openDetails: 'Abrir detalhes', close: 'Fechar', filterAll: 'Todos', filterThesis: 'Núcleo da tese', filterEmerging: 'Foco em EMDEs', filterImplementation: 'Implementação', filterData: 'Dados e supervisão', byPublisher: 'Instituição', byJurisdiction: 'Jurisdição', byYear: 'Ano', featuredJurisdictions: 'Jurisdições destacadas', relatedExamples: 'Exemplos', themePerspective: 'Por que importa', researchSignals: 'Sinais de pesquisa'
  }
};

const ISO3_TO_ISO2 = {
  BRA:'BR', EUU:null, USA:'US', GBR:'GB', SGP:'SG', HKG:'HK', CHE:'CH', ARE:'AE', SLV:'SV', PRY:'PY',
  DEU:'DE', FRA:'FR', ARG:'AR', CHL:'CL', NGA:'NG', CHN:'CN', IND:'IN', JPN:'JP', KOR:'KR', AUS:'AU', CAN:'CA',
  BHS:'BS', BMU:'BM', CYM:'KY', GIB:'GI', ISR:'IL', LIE:'LI', MUS:'MU', ZAF:'ZA', IDN:'ID', MEX:'MX', PHL:'PH', THA:'TH', NOR:'NO', KEN:'KE', TWN:'TW', TUR:'TR', UKR:'UA', SAU:'SA', QAT:'QA',
  ZAF:'ZA'
};

const state = { lang: localStorage.getItem('atlas-lang') || 'pt', countries: [], filtered: [], map: null, selected: null, view: 'map', metadata: null, themes: [], updates: [], experiences: [], library: [], timeline: [], experienceFilter: 'all', libraryFilter: 'all', libraryThemeFilter: 'all', libraryJurisdictionFilter: 'all', timelineJurisdiction: 'all', timelineYear: 'all', timelineCategory: 'all' };
const REGION_ORDER = ['Africa', 'Asia', 'Europe', 'Latin America', 'Middle East', 'North America', 'Oceania'];
const CRITERIA_LABELS = {
  legal_certainty: { en: 'Legal certainty', pt: 'Segurança jurídica' },
  proportionality: { en: 'Proportionality', pt: 'Proporcionalidade' },
  exchanges: { en: 'Exchanges', pt: 'Exchanges' },
  stablecoins: { en: 'Stablecoins', pt: 'Stablecoins' },
  tokenization: { en: 'Tokenization', pt: 'Tokenização' },
  taxation: { en: 'Taxation', pt: 'Tributação' },
  innovation_openness: { en: 'Innovation openness', pt: 'Abertura à inovação' },
  anti_centralization: { en: 'Anti-centralization', pt: 'Anticentralização' },
  mining_infrastructure: { en: 'Mining infrastructure', pt: 'Infraestrutura de mineração' }
};

const MARKER_COORDS = {
  EUU: { lat: 50.8503, lng: 4.3517, label_en: 'European Union', label_pt: 'União Europeia' },
  SGP: { lat: 1.3521, lng: 103.8198, label_en: 'Singapore', label_pt: 'Singapura' },
  HKG: { lat: 22.3193, lng: 114.1694, label_en: 'Hong Kong', label_pt: 'Hong Kong' },
  ARE: { lat: 24.4539, lng: 54.3773, label_en: 'United Arab Emirates', label_pt: 'Emirados Árabes Unidos' },
  SLV: { lat: 13.7942, lng: -88.8965, label_en: 'El Salvador', label_pt: 'El Salvador' },
  PRY: { lat: -23.4425, lng: -58.4438, label_en: 'Paraguay', label_pt: 'Paraguai' },
  BHS: { lat: 25.0343, lng: -77.3963, label_en: 'Bahamas', label_pt: 'Bahamas' },
  BMU: { lat: 32.3078, lng: -64.7505, label_en: 'Bermuda', label_pt: 'Bermuda' },
  CYM: { lat: 19.3133, lng: -81.2546, label_en: 'Cayman Islands', label_pt: 'Ilhas Cayman' },
  GIB: { lat: 36.1408, lng: -5.3536, label_en: 'Gibraltar', label_pt: 'Gibraltar' },
  LIE: { lat: 47.1660, lng: 9.5554, label_en: 'Liechtenstein', label_pt: 'Liechtenstein' },
  MUS: { lat: -20.3484, lng: 57.5522, label_en: 'Mauritius', label_pt: 'Maurício' },
  QAT: { lat: 25.2854, lng: 51.5310, label_en: 'Qatar', label_pt: 'Catar' }
};
const MARKER_ONLY = new Set(['EUU','BHS','BMU','CYM','GIB','LIE','MUS','QAT']);

function t(key) { return I18N[state.lang][key] || key; }
function tf(key, vars={}) { let s=t(key); Object.entries(vars).forEach(([k,v])=>{ s=s.replace(`{${k}}`, String(v));}); return s; }
function esc(s='') { return String(s).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])); }
function colorForScore(score) { if (score >= 80) return '#14532d'; if (score >= 65) return '#16a34a'; if (score >= 50) return '#eab308'; if (score >= 35) return '#f97316'; return '#b91c1c'; }
function translateClassification(value) { return t(`class_${value}`); }
function translateTrend(value) { return t(`t_${value}`); }
function translateStatus(value='') { return t(String(value).toLowerCase()) || value; }
function fmtDate(d){ return d ? new Date(d).toLocaleDateString(state.lang === 'pt' ? 'pt-BR' : 'en-GB') : '—'; }
function labelForCriterion(key){ return CRITERIA_LABELS[key]?.[state.lang] || key.replace(/_/g, ' '); }
function loadJson(path){ return fetch(path).then(r => { if(!r.ok) throw new Error(`Failed to load ${path}`); return r.json(); }); }

function applyTranslations() {
  document.documentElement.lang = state.lang;
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const key = el.dataset.i18n;
    if (el.tagName === 'INPUT' && el.placeholder !== undefined) el.placeholder = t(key);
    else el.textContent = t(key);
  });
  document.getElementById('search-input').placeholder = t('searchPlaceholder');
  const navLinks = [['#overview-section','overviewNav'],['#experiences-section','experiencesNav'],['#themes-section','themesNav'],['#map-section','mapNav'],['#compare-section','compareNav'],['#library-section','libraryNav'],['#timeline-section','timelineNav'],['#methodology-section','methodologyNav']];
  document.querySelectorAll('.nav-link').forEach((el, idx) => el.textContent = t(navLinks[idx][1]));
  const closeBtn=document.getElementById('drawer-close'); if (closeBtn) closeBtn.textContent=t('close');
  renderAll();
}

function setupLanguage() {
  document.querySelectorAll('.lang-btn').forEach(btn => {
    btn.classList.toggle('active', btn.dataset.lang === state.lang);
    btn.addEventListener('click', () => {
      state.lang = btn.dataset.lang; localStorage.setItem('atlas-lang', state.lang);
      document.querySelectorAll('.lang-btn').forEach(x => x.classList.toggle('active', x.dataset.lang === state.lang));
      applyTranslations();
    });
  });
}

function populateRegions() {
  const select = document.getElementById('region-filter');
  const regions = [...new Set(state.countries.map(c => c.region))].sort((a,b)=>REGION_ORDER.indexOf(a)-REGION_ORDER.indexOf(b));
  select.innerHTML = `<option value="all">${t('allRegions')}</option>` + regions.map(r => `<option value="${esc(r)}">${esc(r)}</option>`).join('');
  populateCompareSelects();
  populateTimelineFilters();
  populateLibraryFilters();
}

function populateCompareSelects() {
  const options = state.countries.slice().sort((a,b)=>a.country.localeCompare(b.country)).map(c => `<option value="${esc(c.iso3)}">${esc(c.country)}</option>`).join('');
  ['compare-a','compare-b'].forEach((id, idx) => {
    const el = document.getElementById(id);
    if (!el) return;
    el.innerHTML = options;
    el.value = idx === 0 ? 'BRA' : 'USA';
    el.onchange = renderCompare;
  });
}


function populateTimelineFilters() {
  const j = document.getElementById('timeline-jurisdiction-filter');
  const y = document.getElementById('timeline-year-filter');
  const c = document.getElementById('timeline-category-filter');
  if (j) {
    const jurisdictionOptions = state.countries.slice().sort((a,b)=>a.country.localeCompare(b.country)).map(c=>`<option value="${esc(c.iso3)}">${esc(c.country)}</option>`).join('');
    j.innerHTML = `<option value="all">${t('filterAll')}</option>` + jurisdictionOptions;
    j.value = state.timelineJurisdiction;
  }
  if (y) {
    const years = [...new Set((state.timeline||[]).map(ev => String(ev.date||'').slice(0,4)).filter(Boolean))].sort((a,b)=>b.localeCompare(a));
    y.innerHTML = `<option value="all">${t('filterAll')}</option>` + years.map(yr=>`<option value="${yr}">${yr}</option>`).join('');
    y.value = state.timelineYear;
  }
  if (c) {
    const categories = [...new Set((state.timeline||[]).map(ev => ev.category).filter(Boolean))].sort();
    c.innerHTML = `<option value="all">${t('filterAll')}</option>` + categories.map(cat=>`<option value="${esc(cat)}">${esc(cat)}</option>`).join('');
    c.value = state.timelineCategory;
  }
}

function populateLibraryFilters() {
  const l = document.getElementById('library-filter');
  const lt = document.getElementById('library-theme-filter');
  const lj = document.getElementById('library-jurisdiction-filter');
  if (l) {
    const pubs = [...new Set((state.library||[]).map(item => item.publisher).filter(Boolean))].sort();
    l.innerHTML = `<option value="all">${t('filterAll')}</option>` + pubs.map(p=>`<option value="${esc(p)}">${esc(p)}</option>`).join('');
    l.value = state.libraryFilter;
  }
  if (lt) {
    const themes = [...new Set((state.library||[]).flatMap(item => item.themes || []).filter(Boolean))].sort();
    lt.innerHTML = `<option value="all">${t('filterAll')}</option>` + themes.map(tag=>`<option value="${esc(tag)}">${esc(tag)}</option>`).join('');
    lt.value = state.libraryThemeFilter;
  }
  if (lj) {
    const jurisdictions = [...new Set((state.library||[]).flatMap(item => item.jurisdictions || []).filter(Boolean))]
      .map(iso => state.countries.find(c => c.iso3 === iso)).filter(Boolean).sort((a,b)=>a.country.localeCompare(b.country));
    lj.innerHTML = `<option value="all">${t('filterAll')}</option>` + jurisdictions.map(c=>`<option value="${esc(c.iso3)}">${esc(c.country)}</option>`).join('');
    lj.value = state.libraryJurisdictionFilter;
  }
}

function openDrawer(title, html) {
  const overlay = document.getElementById('overlay');
  const drawer = document.getElementById('drawer');
  document.getElementById('drawer-title').textContent = title;
  document.getElementById('drawer-content').innerHTML = html;
  overlay.classList.remove('hidden'); drawer.classList.remove('hidden');
}
function closeDrawer(){ document.getElementById('overlay')?.classList.add('hidden'); document.getElementById('drawer')?.classList.add('hidden'); }
function attachDrawerEvents(){ document.getElementById('overlay')?.addEventListener('click', closeDrawer); document.getElementById('drawer-close')?.addEventListener('click', closeDrawer); }

function applyFilters() {
  const q = document.getElementById('search-input').value.trim().toLowerCase();
  const region = document.getElementById('region-filter').value;
  const trend = document.getElementById('trend-filter').value;
  const scoreMin = Number(document.getElementById('score-min').value || 0);
  const topic = document.getElementById('topic-filter').value;
  state.filtered = state.countries.filter(c => {
    if (q && !(`${c.country} ${c.summary_en} ${c.summary_pt}`.toLowerCase().includes(q))) return false;
    if (region !== 'all' && c.region !== region) return false;
    if (trend !== 'all' && c.trend !== trend) return false;
    if (c.score < scoreMin) return false;
    if (topic !== 'all' && !c.status?.[topic]) return false;
    return true;
  });
  if (state.selected && !state.filtered.some(c => c.iso3 === state.selected.iso3)) state.selected = state.filtered[0] || state.countries[0] || null;
  renderAll();
}

function renderRanking() {
  const favorable = [...state.countries].filter(c => c.score >= 65).sort((a,b)=>b.score-a.score).slice(0,10);
  const restrictive = [...state.countries].filter(c => c.score < 65).sort((a,b)=>a.score-b.score).slice(0,10);
  document.getElementById('top-favorable').innerHTML = favorable.map(c => `<li>${esc(c.country)} <strong>${c.score}</strong></li>`).join('') || `<li>${t('noData')}</li>`;
  document.getElementById('top-restrictive').innerHTML = restrictive.map(c => `<li>${esc(c.country)} <strong>${c.score}</strong></li>`).join('') || `<li>${t('noData')}</li>`;
}

function renderUpdates() {
  const list = state.updates.length ? state.updates.slice(0,8).map(u => {
    const title = state.lang === 'pt' ? (u.title_pt || u.country || '') : (u.title_en || u.country || '');
    const summary = state.lang === 'pt' ? (u.summary_pt || '') : (u.summary_en || '');
    return `<li><strong>${esc(title)}</strong> — ${fmtDate(u.date || u.last_update)}<br><span>${esc(summary)}</span></li>`;
  }) : [...state.countries].sort((a,b)=>String(b.last_update).localeCompare(String(a.last_update))).slice(0,8).map(c => `<li><strong>${esc(c.country)}</strong> — ${fmtDate(c.last_update)}<br><span>${translateTrend(c.trend)}</span></li>`);
  document.getElementById('latest-updates').innerHTML = list.join('');
}

function renderThemes() {
  const target = document.getElementById('themes-grid');
  if (!target) return;
  target.innerHTML = state.themes.map(theme => {
    const title = state.lang === 'pt' ? theme.title_pt : theme.title_en;
    const summary = state.lang === 'pt' ? theme.summary_pt : theme.summary_en;
    const featured = (theme.countries_featured||[]).map(iso => state.countries.find(x=>x.iso3===iso)).filter(Boolean);
    const chips = featured.map(c=>`<span>${esc(c.country)}</span>`).join('');
    const details = `<p>${esc(summary)}</p><h3>${t('featuredJurisdictions')}</h3><ul>${featured.map(c=>`<li>${esc(c.country)} — ${translateClassification(c.classification)} (${c.score})</li>`).join('')}</ul><h3>${t('themePerspective')}</h3><p>${esc(summary)}</p>`;
    return `<article class="theme-card"><h3>${esc(title)}</h3><p>${esc(summary)}</p><div class="theme-countries">${chips}</div><button class="button-secondary card-action" data-theme="${esc(theme.slug)}">${t('openDetails')}</button></article>`;
  }).join('');
  target.querySelectorAll('[data-theme]').forEach(btn => btn.addEventListener('click', () => {
    const theme = state.themes.find(x=>x.slug===btn.dataset.theme); if (!theme) return;
    const title = state.lang==='pt'?theme.title_pt:theme.title_en; const summary = state.lang==='pt'?theme.summary_pt:theme.summary_en;
    const featured=(theme.countries_featured||[]).map(iso => state.countries.find(x=>x.iso3===iso)).filter(Boolean);
    openDrawer(title, `<p>${esc(summary)}</p><h3>${t('featuredJurisdictions')}</h3><ul>${featured.map(c=>`<li><strong>${esc(c.country)}</strong> — ${translateClassification(c.classification)} (${c.score})</li>`).join('')}</ul>`);
  }));
}

function renderExperiences() {
  const title = document.getElementById('experiences-title');
  if (title) title.textContent = t('experiencesTitle');
  const filter = state.experienceFilter;
  const target = document.getElementById('experiences-grid');
  if (!target) return;
  const list = (state.experiences || []).filter(exp => {
    const examples=(exp.examples||[]).map(iso=>state.countries.find(c=>c.iso3===iso)).filter(Boolean);
    if (filter === 'thesis') return examples.some(c=>c.thesis_core);
    if (filter === 'emerging') return examples.some(c=>['Latin America','Africa','Asia','Middle East'].includes(c.region) && c.score < 65);
    if (filter === 'implementation') return (exp.categories || []).includes('implementation');
    if (filter === 'data') return (exp.categories || []).includes('data') || /atlas|monitor/i.test((exp.title_en||'') + ' ' + (exp.summary_en||'') + ' ' + (exp.title_pt||'') + ' ' + (exp.summary_pt||''));
    return true;
  });
  target.innerHTML = list.map((exp, idx) => {
    const ttl = state.lang === 'pt' ? exp.title_pt : exp.title_en;
    const summary = state.lang === 'pt' ? exp.summary_pt : exp.summary_en;
    const chips = (exp.examples || []).map(iso => state.countries.find(x => x.iso3 === iso)).filter(Boolean).map(c => `<span>${esc(c.country)}</span>`).join('');
    const tags = (exp.categories || []).map(tag => `<span>${esc(tag)}</span>`).join('');
    return `<article class="experience-card"><h3>${esc(ttl)}</h3><p>${esc(summary)}</p><div class="experience-tags">${tags}</div><div class="theme-countries">${chips}</div><button class="button-secondary card-action" data-experience="${idx}">${t('openDetails')}</button></article>`;
  }).join('') || `<p>${t('noData')}</p>`;
  target.querySelectorAll('[data-experience]').forEach(btn => btn.addEventListener('click', () => {
    const exp = list[Number(btn.dataset.experience)]; if (!exp) return;
    const ttl = state.lang==='pt'?exp.title_pt:exp.title_en;
    const summary = state.lang==='pt'?exp.summary_pt:exp.summary_en;
    const examples=(exp.examples||[]).map(iso => state.countries.find(x=>x.iso3===iso)).filter(Boolean);
    const tags = (exp.categories || []).map(tag => `<span>${esc(tag)}</span>`).join('');
    const sources = (exp.sources || []).map(s => `<li>${esc(s)}</li>`).join('');
    openDrawer(ttl, `<p>${esc(summary)}</p><div class="library-tags">${tags}</div><h3>${t('relatedExamples')}</h3><ul>${examples.map(c=>`<li><strong>${esc(c.country)}</strong> — ${translateClassification(c.classification)} (${c.score})</li>`).join('')}</ul>${sources ? `<h3>${t('sources')}</h3><ul>${sources}</ul>` : ''}`);
  }));
}

function renderLibrary() {
  const title = document.getElementById('library-title');
  if (title) title.textContent = t('libraryTitle');
  const target = document.getElementById('library-grid');
  if (!target) return;
  const list = (state.library || []).filter(item => {
    if (state.libraryFilter !== 'all' && item.publisher !== state.libraryFilter) return false;
    if (state.libraryThemeFilter !== 'all' && !(item.themes || []).includes(state.libraryThemeFilter)) return false;
    if (state.libraryJurisdictionFilter !== 'all' && !(item.jurisdictions || []).includes(state.libraryJurisdictionFilter)) return false;
    return true;
  });
  target.innerHTML = `<div class="source-list">` + list.map(item => {
    const focus = state.lang === 'pt' ? item.focus_pt : item.focus_en;
    const tags = (item.themes || []).map(tag => `<span>${esc(tag)}</span>`).join('');
    const jurs = (item.jurisdictions || []).map(iso => state.countries.find(c => c.iso3 === iso)).filter(Boolean).slice(0,6).map(c => `<span>${esc(c.country)}</span>`).join('');
    return `<article class="library-card"><h3>${esc(item.title)}</h3><p class="library-meta">${esc(item.publisher)} · ${esc(item.year)} · ${esc(item.type || 'report')}</p><p>${esc(focus)}</p><div class="library-tags">${tags}</div><div class="theme-countries">${jurs}</div><button class="button-secondary card-action" data-library="${esc(item.title)}">${t('openDetails')}</button></article>`;
  }).join('') + `</div>`;
  target.querySelectorAll('[data-library]').forEach(btn => btn.addEventListener('click', () => {
    const item = list.find(x=>x.title===btn.dataset.library); if (!item) return;
    const focus = state.lang==='pt'?item.focus_pt:item.focus_en;
    const why = state.lang==='pt'?item.why_pt:item.why_en;
    const jurs = (item.jurisdictions || []).map(iso => state.countries.find(c => c.iso3 === iso)).filter(Boolean);
    openDrawer(item.title, `<p class="source-meta">${esc(item.publisher)} · ${esc(item.year)} · ${esc(item.type || 'report')}</p><p>${esc(focus)}</p><h3>${t('researchSignals')}</h3><p>${esc(why || '')}</p><div class="mini-grid"><div class="mini-card"><strong>${t('topic')}</strong><div class="library-tags">${(item.themes || []).map(tag => `<span>${esc(tag)}</span>`).join('')}</div></div><div class="mini-card"><strong>${t('relatedExamples')}</strong><ul>${jurs.map(c=>`<li>${esc(c.country)}</li>`).join('') || `<li>${t('noData')}</li>`}</ul></div></div>`);
  }));
}

function renderTimeline() {
  const title = document.getElementById('timeline-title');
  if (title) title.textContent = t('timelineTitle');
  const target = document.getElementById('timeline-list');
  if (!target) return;
  const summary = document.getElementById('timeline-summary');
  const list = (state.timeline || []).filter(ev => (state.timelineJurisdiction === 'all' || ev.jurisdiction === state.timelineJurisdiction) && (state.timelineYear === 'all' || String(ev.date||'').slice(0,4) === state.timelineYear) && (state.timelineCategory === 'all' || ev.category === state.timelineCategory)).sort((a,b)=>String(b.date).localeCompare(String(a.date)));
  if (summary) summary.innerHTML = `<span>${list.length} events</span><span>•</span><span>${[...new Set(list.map(ev => String(ev.date||'').slice(0,4)))].length} years</span><span>•</span><span>${[...new Set(list.map(ev => ev.category).filter(Boolean))].length} categories</span>`;
  const grouped = list.reduce((acc, ev) => { const y = String(ev.date || '').slice(0,4) || 'Unknown'; (acc[y] ||= []).push(ev); return acc; }, {});
  target.innerHTML = Object.entries(grouped).sort((a,b)=>b[0].localeCompare(a[0])).map(([year, events]) => `<div class="timeline-year-group"><div class="timeline-year">${esc(year)}</div>${events.map(ev => { const ttl = state.lang === 'pt' ? ev.title_pt : ev.title_en; const c = state.countries.find(x => x.iso3 === ev.jurisdiction); return `<article class="timeline-entry" data-date="${esc(ev.date)}" data-jurisdiction="${esc(ev.jurisdiction || '')}"><div class="timeline-top"><div><div class="timeline-date">${fmtDate(ev.date)}</div><div class="timeline-title">${esc(ttl)}</div></div><div class="timeline-category-badge"><span>${esc(ev.category || 'general')}</span></div></div><div class="timeline-meta">${c ? `<span class="timeline-tag">${esc(c.country)}</span>` : ''}${ev.source ? `<span class="timeline-tag">${esc(ev.source)}</span>` : ''}</div></article>`; }).join('')}</div>`).join('') || `<div class="timeline-entry">${t('noData')}</div>`;
  target.querySelectorAll('.timeline-entry').forEach(entry => entry.addEventListener('click', () => {
    const ev = list.find(x => x.date === entry.dataset.date && (x.jurisdiction || '') === entry.dataset.jurisdiction); if (!ev) return;
    const ttl = state.lang === 'pt' ? ev.title_pt : ev.title_en;
    const body = state.lang === 'pt' ? (ev.note_pt || '') : (ev.note_en || '');
    const c = state.countries.find(x => x.iso3 === ev.jurisdiction);
    openDrawer(ttl, `${body ? `<p>${esc(body)}</p>` : ''}<div class="mini-grid"><div class="mini-card"><strong>${t('lastUpdate')}</strong>${fmtDate(ev.date)}</div><div class="mini-card"><strong>${t('country')}</strong>${esc(c?.country || ev.jurisdiction || '—')}</div><div class="mini-card"><strong>Category</strong>${esc(ev.category || 'general')}</div><div class="mini-card"><strong>${t('sources')}</strong>${esc(ev.source || 'Atlas')}</div></div>`);
  }));
}

function renderTable() {
  const body = document.getElementById('countries-table-body');
  body.innerHTML = [...state.filtered].sort((a,b)=>b.score-a.score).map(c => `
    <tr data-iso3="${c.iso3}">
      <td>${esc(c.country)} ${c.thesis_core ? '<span class="table-badge">★</span>' : ''}</td>
      <td>${esc(c.region)}</td>
      <td><span class="score-chip" style="background:${colorForScore(c.score)}">${c.score}</span></td>
      <td>${translateClassification(c.classification)}</td>
      <td>${translateTrend(c.trend)}</td>
    </tr>`).join('') || `<tr><td colspan="5">${t('noData')}</td></tr>`;
  body.querySelectorAll('tr[data-iso3]').forEach(tr => tr.addEventListener('click', () => {
    const country = state.countries.find(c => c.iso3 === tr.dataset.iso3); if (country) { state.selected = country; renderCountryPanel(); if (state.view==='map') buildMap(); }
  }));
}

function renderCountryPanel() {
  const panel = document.getElementById('country-panel');
  const country = state.selected;
  if (!country) { panel.innerHTML = `<div class="empty-state"><h2>${t('selectCountry')}</h2><p>${t('selectCountryHelp')}</p></div>`; return; }
  const summary = state.lang === 'pt' ? country.summary_pt : country.summary_en;
  const opportunities = state.lang === 'pt' ? country.opportunities_pt : country.opportunities_en;
  const risks = state.lang === 'pt' ? country.risks_pt : country.risks_en;
  const analysis = state.lang === 'pt' ? country.detailed_analysis_pt : country.detailed_analysis_en;
  const model = state.lang === 'pt' ? country.model_pt : country.model_en;
  const stablecoin = state.lang === 'pt' ? country.stablecoin_regime_pt : country.stablecoin_regime_en;
  const tokenization = state.lang === 'pt' ? country.tokenization_regime_pt : country.tokenization_regime_en;
  const cbdcNote = state.lang === 'pt' ? country.cbdc_note_pt : country.cbdc_note_en;
  const statusPairs = Object.entries(country.status||{}).map(([k,v]) => `<div class="status-item"><div class="label">${t(k)}</div><div>${translateStatus(v)}</div></div>`).join('');
  const criteria = Object.entries(country.criteria||{}).map(([k,v]) => `<li>${esc(labelForCriterion(k))}: <strong>${v}</strong></li>`).join('');
  panel.innerHTML = `
    <div class="detail-header">
      <div>
        <p class="eyebrow">${esc(country.region)}</p>
        <h2>${esc(country.country)}</h2>
        <p>${esc(summary)}</p>
      </div>
      <div class="detail-badges">
        <span class="badge classification" style="background:${colorForScore(country.score)}">${translateClassification(country.classification)}</span>
        ${country.thesis_core ? `<span class="badge thesis">${t('thesisBadge')}</span>` : ''}
      </div>
    </div>
    <div class="kpi-grid">
      <div class="kpi"><div class="label">ISO3</div><div>${country.iso3}</div></div>
      <div class="kpi"><div class="label">${t('trend')}</div><div>${translateTrend(country.trend)}</div></div>
      <div class="kpi"><div class="label">${t('region')}</div><div>${esc(country.region)}</div></div>
      <div class="kpi"><div class="label">${t('lastUpdate')}</div><div>${fmtDate(country.last_update)}</div></div>
    </div>
    <h3>${t('statusOverview')}</h3>
    <div class="status-grid">${statusPairs}</div>
    <h3>${t('criteriaBreakdown')}</h3>
    <ul>${criteria}</ul>
    <h3>${t('regulatoryModel')}</h3><p>${esc(model || t('noData'))}</p>
    <h3>${t('detailedAnalysis')}</h3><p>${esc(analysis || t('noData'))}</p>
    <h3>${t('authorities')}</h3><ul>${(country.authorities||[]).map(x=>`<li>${esc(x)}</li>`).join('') || `<li>${t('noData')}</li>`}</ul>
    <h3>${t('stablecoinRegime')}</h3><p>${esc(stablecoin || t('noData'))}</p>
    <h3>${t('tokenizationRegime')}</h3><p>${esc(tokenization || t('noData'))}</p>
    <h3>${t('cbdcNote')}</h3><p>${esc(cbdcNote || t('noData'))}</p>
    <h3>${t('principalLaws')}</h3><ul>${(country.laws||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul>
    <h3>${t('opportunities')}</h3><ul>${(opportunities||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul>
    <h3>${t('risks')}</h3><ul>${(risks||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul>
    <h3>${t('sources')}</h3><ul>${(country.references||[]).map(r=>`<li><a href="${esc(r.url)}" target="_blank" rel="noopener noreferrer">${esc(r.label)}</a></li>`).join('')}</ul>`;
}

function getMapCode(country) { return ISO3_TO_ISO2[country.iso3] || null; }
function renderMapCoverage(values, markers) {
  const note = document.getElementById('map-coverage-note');
  const list = document.getElementById('map-marker-list');
  if (note) note.textContent = tf('mapCoverageNote', { filled: Object.keys(values).length, markers: markers.length });
  if (list) {
    list.innerHTML = markers.length
      ? `<strong>${t('markerJurisdictions')}:</strong> ` + markers.map(m => `<span class="marker-pill">${esc(m.name)}</span>`).join(' ')
      : '';
  }
}

function buildMap() {
  const values = {};
  state.countries.forEach(c => { const code = getMapCode(c); if (code && !MARKER_ONLY.has(c.iso3)) values[code] = c.score; });
  const markers = state.countries
    .filter(c => MARKER_COORDS[c.iso3])
    .map(c => ({
      name: state.lang === 'pt' ? (MARKER_COORDS[c.iso3].label_pt || c.country) : (MARKER_COORDS[c.iso3].label_en || c.country),
      coords: [MARKER_COORDS[c.iso3].lat, MARKER_COORDS[c.iso3].lng],
      iso3: c.iso3,
      score: c.score
    }));
  const selectedCode = state.selected ? getMapCode(state.selected) : null;
  const selectedMarkerIndex = state.selected ? markers.findIndex(m => m.iso3 === state.selected.iso3) : -1;
  if (state.map) state.map.destroy();
  state.map = new jsVectorMap({
    selector: '#world-map', map: 'world', backgroundColor: 'transparent', zoomButtons: true, zoomOnScroll: true,
    visualizeData: { scale: ['#b91c1c','#f97316','#eab308','#16a34a','#14532d'], values },
    regionStyle: { initial: { fill: '#cbd5e1', fillOpacity: 1, stroke: '#fff', strokeWidth: 0.7 }, selected: { fill: '#1d4ed8' } },
    markers,
    markerStyle: {
      initial: { r: 5, fill: '#0f172a', stroke: '#ffffff', strokeWidth: 1.5 },
      hover: { fill: '#1d4ed8' },
      selected: { fill: '#1d4ed8', r: 6 }
    },
    selectedRegions: selectedCode ? [selectedCode] : [],
    selectedMarkers: selectedMarkerIndex >= 0 ? [selectedMarkerIndex] : [],
    onRegionTooltipShow(_, tooltip, code) {
      const country = state.countries.find(c => getMapCode(c) === code);
      if (!country) { tooltip.text(`${code}: ${t('noData')}`); return; }
      const summary = state.lang === 'pt' ? country.summary_pt : country.summary_en;
      tooltip.html(`<div style="max-width:260px"><strong>${esc(country.country)}</strong><br>${t('score')}: ${country.score}<br>${t('classification')}: ${translateClassification(country.classification)}<br>${t('trend')}: ${translateTrend(country.trend)}<br><span style="color:#64748b">${esc(summary.slice(0,120))}...</span></div>`);
    },
    onMarkerTooltipShow(_, tooltip, index) {
      const marker = markers[index];
      const country = state.countries.find(c => c.iso3 === marker.iso3);
      if (!country) { tooltip.text(`${t('markerTooltip')}: ${marker.name}`); return; }
      const summary = state.lang === 'pt' ? country.summary_pt : country.summary_en;
      tooltip.html(`<div style="max-width:260px"><strong>${esc(country.country)}</strong><br>${t('score')}: ${country.score}<br>${t('classification')}: ${translateClassification(country.classification)}<br>${t('trend')}: ${translateTrend(country.trend)}<br><span style="color:#64748b">${esc(summary.slice(0,120))}...</span></div>`);
    },
    onRegionClick(_, code) {
      const country = state.countries.find(c => getMapCode(c) === code);
      if (country) { state.selected = country; renderCountryPanel(); buildMap(); }
    },
    onMarkerClick(_, index) {
      const marker = markers[index];
      const country = state.countries.find(c => c.iso3 === marker.iso3);
      if (country) { state.selected = country; renderCountryPanel(); buildMap(); }
    }
  });
  renderMapCoverage(values, markers);
}

function renderMetadata(meta) {
  state.metadata = meta;
  const dt = meta?.generated_at ? new Date(meta.generated_at).toLocaleString(state.lang === 'pt' ? 'pt-BR' : 'en-GB') : '—';
  const countriesCovered = meta?.country_count ?? state.countries.length;
  document.getElementById('metadata').textContent = `${t('generatedAt')}: ${dt} • ${t('countriesCovered')}: ${countriesCovered}`;
  document.getElementById('stat-jurisdictions').textContent = countriesCovered;
  const acts = state.countries.reduce((sum,c)=>sum + (c.laws?.length || 0), 0);
  document.getElementById('stat-acts').textContent = acts;
  document.getElementById('stat-themes').textContent = meta?.theme_count ?? state.themes.length;
  document.getElementById('stat-thesis').textContent = meta?.thesis_core_count ?? state.countries.filter(c=>c.thesis_core).length;
}

function renderCompare() {
  const a = state.countries.find(c => c.iso3 === document.getElementById('compare-a')?.value);
  const b = state.countries.find(c => c.iso3 === document.getElementById('compare-b')?.value);
  const body = document.getElementById('compare-body');
  if (!a || !b || !body) return;
  const rows = [
    [t('country'), a.country, b.country],
    [t('classification'), translateClassification(a.classification), translateClassification(b.classification)],
    [t('trend'), translateTrend(a.trend), translateTrend(b.trend)],
    [t('summary'), state.lang==='pt'?a.summary_pt:a.summary_en, state.lang==='pt'?b.summary_pt:b.summary_en],
    [t('regulatoryModel'), state.lang==='pt'?a.model_pt:a.model_en, state.lang==='pt'?b.model_pt:b.model_en],
    [t('exchanges'), translateStatus(a.status?.exchanges), translateStatus(b.status?.exchanges)],
    [t('stablecoins'), translateStatus(a.status?.stablecoins), translateStatus(b.status?.stablecoins)],
    [t('tokenization'), translateStatus(a.status?.tokenization), translateStatus(b.status?.tokenization)],
    [t('cbdc'), translateStatus(a.status?.cbdc), translateStatus(b.status?.cbdc)],
    [t('stablecoinRegime'), state.lang==='pt'?a.stablecoin_regime_pt:a.stablecoin_regime_en, state.lang==='pt'?b.stablecoin_regime_pt:b.stablecoin_regime_en],
    [t('tokenizationRegime'), state.lang==='pt'?a.tokenization_regime_pt:a.tokenization_regime_en, state.lang==='pt'?b.tokenization_regime_pt:b.tokenization_regime_en],
    [t('authorities'), (a.authorities||[]).join(', '), (b.authorities||[]).join(', ')],
    [t('principalLaws'), (a.laws||[]).join(', '), (b.laws||[]).join(', ')],
    ['Score', String(a.score), String(b.score)]
  ];
  body.innerHTML = rows.map(([label,va,vb]) => `<tr><th>${esc(label)}</th><td>${esc(va||t('noData'))}</td><td>${esc(vb||t('noData'))}</td></tr>`).join('');
}

function renderAll() {
  document.getElementById('score-min-value').textContent = document.getElementById('score-min').value;
  renderRanking();
  renderUpdates();
  renderThemes();
  renderExperiences();
  renderLibrary();
  renderTimeline();
  renderTable();
  renderCountryPanel();
  renderCompare();
  if (state.metadata) renderMetadata(state.metadata);
  if (document.getElementById('world-map') && state.view==='map') {
    try {
      buildMap();
    } catch (err) {
      console.error('Map rendering failed:', err);
      const note = document.getElementById('map-coverage-note');
      if (note) note.textContent = state.lang === 'pt' ? 'Falha ao renderizar o mapa. Verifique a carga da biblioteca de mapa.' : 'Failed to render map. Check the map library loading.';
    }
  }
}

function setupControls() {
  ['search-input','region-filter','trend-filter','score-min','topic-filter'].forEach(id => document.getElementById(id).addEventListener('input', applyFilters));
  document.getElementById('reset-filters').addEventListener('click', () => {
    document.getElementById('search-input').value = '';
    document.getElementById('region-filter').value = 'all';
    document.getElementById('trend-filter').value = 'all';
    document.getElementById('score-min').value = '0';
    document.getElementById('topic-filter').value = 'all';
    applyFilters();
  });
  document.getElementById('experience-filter')?.addEventListener('change', e => { state.experienceFilter = e.target.value; renderExperiences(); });
  document.getElementById('library-filter')?.addEventListener('change', e => { state.libraryFilter = e.target.value; renderLibrary(); });
  document.getElementById('library-theme-filter')?.addEventListener('change', e => { state.libraryThemeFilter = e.target.value; renderLibrary(); });
  document.getElementById('library-jurisdiction-filter')?.addEventListener('change', e => { state.libraryJurisdictionFilter = e.target.value; renderLibrary(); });
  document.getElementById('timeline-jurisdiction-filter')?.addEventListener('change', e => { state.timelineJurisdiction = e.target.value; renderTimeline(); });
  document.getElementById('timeline-year-filter')?.addEventListener('change', e => { state.timelineYear = e.target.value; renderTimeline(); });
  document.getElementById('timeline-category-filter')?.addEventListener('change', e => { state.timelineCategory = e.target.value; renderTimeline(); });
  document.querySelectorAll('.view-btn').forEach(btn => btn.addEventListener('click', () => {
    state.view = btn.dataset.view;
    document.querySelectorAll('.view-btn').forEach(x=>x.classList.toggle('active', x===btn));
    document.getElementById('map-section').classList.toggle('hidden', state.view !== 'map');
    document.getElementById('table-section').classList.toggle('hidden', state.view !== 'table');
    if (state.view === 'map') setTimeout(buildMap, 50);
  }));
}

async function init() {
  setupLanguage(); setupControls(); attachDrawerEvents();
  const [countries, metadata, themesFile, updates, experiencesFile, libraryFile, timelineFile] = await Promise.all([
    loadJson('api/countries.json'),
    loadJson('api/metadata.json'),
    loadJson('api/themes.json').catch(()=>({themes:[]})),
    loadJson('api/updates.json').catch(()=>[]),
    loadJson('api/experiences.json').catch(()=>({experiences:[]})),
    loadJson('api/library.json').catch(()=>({items:[]})),
    loadJson('api/timeline.json').catch(()=>({events:[]}))
  ]);
  state.countries = countries; state.filtered = countries; state.selected = countries.find(c=>c.iso3==='BRA') || countries[0] || null; state.metadata = metadata; state.themes = themesFile.themes || []; state.updates = updates || []; state.experiences = experiencesFile.experiences || []; state.library = libraryFile.items || []; state.timeline = timelineFile.events || [];
  populateRegions(); applyTranslations();
}

init().catch(err => {
  console.error(err);
  document.getElementById('country-panel').innerHTML = `<div class="empty-state"><h2>Error</h2><p>${esc(err.message)}</p></div>`;
});
